<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shara">
    <uses-permission android:name="android.permission.INTERNET" /> <!-- this line adds the required permission request -->


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Wonder">
        <activity
            android:name="com.example.shara.controllers.activities.RegistrationActivity"
            android:theme="@style/Wonder.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.shara.controllers.activities.MainActivity" />
        <activity
            android:name="com.example.shara.controllers.activities.SignInActivity"
            android:parentActivityName="com.example.shara.controllers.activities.MainActivity"
            android:label="eee"
            android:theme="@style/Wonder.Detail"/>
        <activity
            android:name="com.example.shara.controllers.activities.LoginActivity"
            android:parentActivityName="com.example.shara.controllers.activities.RegistrationActivity"
            android:label="S'authentifier"
            android:theme="@style/Wonder.Detail"/>
        <activity
            android:name="com.example.shara.Activity1"
            android:parentActivityName="com.example.shara.controllers.activities.MainActivity"
            android:label="eee"
            android:theme="@style/Wonder.Detail"/>
        <activity
            android:name="com.example.shara.controllers.activities.Compte"
            android:parentActivityName="com.example.shara.controllers.activities.MainActivity"
            android:label="Mon compte"
            android:theme="@style/Wonder.Detail"/>
        <activity
            android:name="com.example.shara.controllers.activities.CreateObjet"
            android:parentActivityName="com.example.shara.controllers.activities.MainActivity"
            android:label="Créer un objet"
            android:theme="@style/Wonder.Detail"/>
    </application>

</manifest>